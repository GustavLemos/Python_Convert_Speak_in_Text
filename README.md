# Python_Convert_Speak_in_Text
Service cognitive python convert speak in text.  

Dependences: 
  * Python;
  * pip install pipwin;
  * pipwin install pyaudio;
  * pip install winspeech;
  * pip install SpeechRecognition;
  * pip install pyaudio.
